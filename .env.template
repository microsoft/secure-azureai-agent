# ============================================
# 🔧 ハンズオン設定: 以下を個人の環境に合わせて変更してください
# ============================================

# Azure OpenAI Configuration (AI Foundryポータルで確認)
AZURE_OPENAI_ENDPOINT=<your-azure-openai-endpoint>         # 🔧 変更必要: AI FoundryのEndpoint URL
AZURE_OPENAI_API_KEY=<your-azure-openai-api-key>           # 🔧 変更必要: AI FoundryのAPIキー

# Azure Key Vault Configuration (Key Vaultが存在する場合)
AZURE_KEY_VAULT_URL=<your-key-vault-url>                   # 🔧 変更必要: Key VaultのURL

# Azure Client ID for Managed Identity (Managed Identityが存在する場合)
AZURE_CLIENT_ID=<your-managed-identity-client-id>          # 🔧 変更必要: Managed IdentityのClient ID

# Application Configuration (個人のApp Service名に合わせて変更)
FRONTEND_URL=<your-frontend-url>                           # 🔧 変更必要: フロントエンドApp ServiceのURL
BACKEND_API_URL=<your-backend-api-url>                     # 🔧 変更必要: バックエンドApp ServiceのURL
ENVIRONMENT=production

# Application Insights (Application Insightsが存在する場合)
APPLICATIONINSIGHTS_CONNECTION_STRING=<your-app-insights-connection-string>  # 🔧 変更必要: App Insights接続文字列

# Azure Subscription and Resource Group (あなたの環境の情報)
AZURE_SUBSCRIPTION_ID=<your-subscription-id>              # 🔧 変更必要: サブスクリプションID
AZURE_RESOURCE_GROUP=<your-resource-group-name>           # 🔧 変更必要: Resource Group名
AZURE_LOCATION=eastus                                      # 🔧 変更必要: 使用するAzureリージョン
