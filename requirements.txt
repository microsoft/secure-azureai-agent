# Combined requirements for the secure-azureai-agent project
# This file consolidates dependencies from backend/, frontend/, and eval/ directories

# Backend dependencies (FastAPI backend service)
fastapi==0.116.1
uvicorn==0.35.0
gunicorn==23.0.0
uvicorn[standard]==0.35.0
semantic-kernel==1.36.0
azure-identity==1.24.0
azure-keyvault-secrets==4.7.0
azure-monitor-opentelemetry==1.6.7
opentelemetry-api==1.34.1
opentelemetry-sdk==1.34.1
opentelemetry-instrumentation-fastapi==0.55b1
pydantic==2.11.7
python-multipart==0.0.20
httpx==0.28.1
python-dotenv==1.1.1

# Frontend dependencies (Chainlit frontend)
chainlit==2.7.1.1

# Evaluation dependencies
azure-ai-evaluation[redteam]

# Note: Frontend dependencies like httpx, python-dotenv, and pydantic are already included above
# from backend dependencies, so they don't need to be duplicated here.
